import Heading from "./heading";

const Footer = ({ className }) => (
    <footer className={ className }>
        <Heading>
            © 2022 nextjs, Inc.
        </Heading>
    </footer>
);

export default Footer;
